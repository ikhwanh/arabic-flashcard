{
  "name": "arabic-flashcard",
  "private": true,
  "version": "2.2.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "generate-manifest": "node scripts/generate-manifest.js",
    "generate-qs-manifest": "node scripts/generate-qs-manifest.js"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  }
}
